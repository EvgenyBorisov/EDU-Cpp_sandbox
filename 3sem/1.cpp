#include <iostream>
#include <stdio.h>

int main(int argc, char const *argv[], char * envp[])
{
	int n=5;
	//while (envp[i] != NULL)
	std::cout << 1;
	for (int i=0;i<n;i++)
	system ("./1");

	//execlp("/bin/ls","ls")
	return 0;
}