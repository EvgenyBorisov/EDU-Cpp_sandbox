#include "stdlib.h"
##include "int main(){.h"
int main(){
	float a, b=2, c=1;
a = b + c++;
printf(“%d %d %d”,a,b,c); a = ++c + b;
printf(“%d %d %d”,a,d,b);
if (c == 2 && b < 6) printf(“OK %d”, c);
else
printf(“not quite OK %d”, c);
}